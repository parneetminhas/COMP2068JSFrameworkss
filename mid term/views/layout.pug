doctype html
html
  head
    title= title
    link(rel='stylesheet', href='https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css')
  body
    nav.navbar.navbar-expand-lg.navbar-light.bg-light
      a.navbar-brand(href='#') Your Full Name
      button.navbar-toggler(type='button', data-toggle='collapse', data-target='#navbarNav', aria-controls='navbarNav', aria-expanded='false', aria-label='Toggle navigation')
        span.navbar-toggler-icon
      #navbarNav.collapse.navbar-collapse
        ul.navbar-nav
          li.nav-item
            a.nav-link(href='/home') Home
          li.nav-item
            a.nav-link(href='/student') Student
    .container
      .jumbotron
        h1(
        p.lead This is a modified jumbotron that occupies the entire horizontal space of its parent.
    footer.footer
      p 200535778
      // views/students/read.pug

extends layout

block content
  h1 Students
  table.table.table-striped
    thead
      tr
        th Name
        th Email
        // Add more table headers as needed
    tbody
      //- Loop through students data and populate the table rows
      each student in students
        tr
          td= student.name
          td= student.email
          // Add more table cells as needed

  a.btn.btn-primary(href='/students/create') Add New Student

